<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lightyear_beroepsproduct3">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfileBestellingActivity"
            android:parentActivityName=".ProfileActivity"
            android:label="Details bestelling"></activity>
        <activity android:name=".ConfiguratorBestellenActivity" />
        <activity android:name=".ConfiguratorVelgActivity" />
        <activity android:name=".ConfiguratorLakActivity" />
        <activity android:name=".ConfiguratorKleurActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ConfiguratorActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>